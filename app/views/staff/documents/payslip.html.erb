<h1>給与明細</h1>

<%= @current_user.name %>

<div>
  <%= "日付" %>|
  <%= "家" %>|
  <%= "開始時間" %>|
  <%= "終了時間" %>|
  <%= "総労働時間" %>|
  <%= "時給" %>|
  <%= "日給" %>|
  <%= "場所" %>|
  <%= "交通費" %>
</div>
<% @works[0].each do |work| %>
  <div>
    <% @current_user.pay_per_hour = 1500 %>

    <%= "家" %>|
    <%= work.start_time.strftime("%H:%M") %>|
    <%= work.end_time.strftime("%H:%M") %>|
    <% working_hours = (work.end_time - work.start_time) / 3600 %>
    <%= working_hours %>|
    <%= @current_user.pay_per_hour %>|
    <% pay_per_day = @current_user.pay_per_hour * working_hours %>
    <%= pay_per_day %>
    <% place = Place.find(work.place_id) %>|
    <%= place.name %>|
    <%= "交通費" %>
  </div>
<% end %>
